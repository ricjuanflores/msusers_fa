Generic single-database configuration with Alembic for FastAPI.

To create a new migration:
    alembic revision --autogenerate -m "description"

To run migrations:
    alembic upgrade head

To rollback:
    alembic downgrade -1

